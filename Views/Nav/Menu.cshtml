
@model IEnumerable<string>

<h2 class="panel text-center text-danger"  style="margin-left:3px; margin-top:60px ; margin-bottom:30px">Categories</h2>
<div style="margin-bottom:20px">
    <!--
        <div style="margin-bottom:7px">
            @Html.ActionLink("Home", "List", "Book", null, new { @class = "btn btn-block btn-default btn-lg" })
        </div>
    -->
    @foreach (var Link in Model)
    {<div style="margin-bottom:7px">
            @Html.RouteLink(Link, new { controller = "Book", action = "List", specilization = Link, pageno = 1 },
           new { @class = "btn btn-block btn-default btn-lg hvr-sweep-to-right" + ((Link == ViewBag.SelectedSpecialization) ? "btn-primary" : "").ToString() })
        </div>
    }
</div>


<button class="btn btn-info btn-lg" 
        style="width:260px; height:50px; font-weight:bold; margin-top:5px"  >
    
    <a href="~/openBook/index.html" target="_blank" style="text-decoration:none; color:white; font-size:25px">
    Free Books
    </a>
</button>


